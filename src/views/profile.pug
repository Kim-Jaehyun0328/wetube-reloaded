extends base
include mixins/video


block content
    div.profile__data
        if !user.avatarUrl
            span 😀
        else
            if user.avatarUrl[0]=="h"
                img.header__avatar(src=loggedInUser.avatarUrl,crossorigin)
            else 
                img.header__avatar(src="/" + user.avatarUrl)
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message No videos found